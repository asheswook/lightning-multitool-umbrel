version: "3.7"

services:

  app_proxy:
    environment:
      APP_HOST: lightning-multitool_core_1
      APP_PORT: 3000
    
  backend:
    image: asheswook/lightning-multitool:v0.1.4-beta
    user: "1000:1000"
    init: true
